<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/style.css">
    <style>
        /* CSS tambahan khusus untuk halaman pesan */
        main { text-align: center; }
        .message-icon { font-size: 4rem; margin-bottom: 1.5rem; color: var(--accent-color); }
        .message-text { font-size: 1.1rem; color: var(--text-color); margin-bottom: 2rem; }
        .message-link {
            display: inline-block;
            padding: 0.8rem 2rem;
            background-color: var(--accent-color);
            color: #0f172a;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
        }
        .message-link:hover {
            background-color: var(--accent-color-hover);
            transform: translateY(-3px);
        }
    </style>
</head>
<body>
    <main>
        <% if (title.toLowerCase().includes('berhasil')) { %>
            <i class="fa-solid fa-circle-check message-icon"></i>
        <% } else { %>
            <i class="fa-solid fa-circle-info message-icon"></i>
        <% } %>
        
        <h1><%= title %></h1>
        <p class="message-text"><%= message %></p>
        <a href="/login" class="message-link">Kembali ke Login</a>
    </main>
</body>
</html>