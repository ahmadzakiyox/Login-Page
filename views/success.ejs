<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Berhasil!</title>
    <link rel="stylesheet" href="/style.css">
    <style>
        /* CSS tambahan khusus untuk halaman sukses */
        main { text-align: center; }
        .success-icon { font-size: 5rem; margin-bottom: 1rem; color: #38b2ac; }
        .welcome-text { margin-top: 0.5rem; margin-bottom: 2rem; font-size: 1.1rem; color: var(--text-muted-color); }
        .logout-button {
            display: inline-block;
            padding: 0.8rem 2rem;
            background-color: transparent;
            color: #e53e3e;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            border: 1px solid #e53e3e;
            transition: all 0.3s;
        }
        .logout-button:hover {
            background-color: #e53e3e;
            color: #fff;
        }
    </style>
</head>
<body>
    <main>
        <div class="success-icon">
            <i class="fa-solid fa-shield-cat"></i>
        </div>
        <h1>Kamu berhasil 🎉</h1>
        <p class="welcome-text">Selamat datang di halaman rahasia.</p>
        <a href="/logout" class="logout-button">Logout</a>
    </main>
</body>
</html>